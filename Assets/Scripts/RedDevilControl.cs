using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RedDevilControl : EnemyCommon
{
    // Start is called before the first frame update
    void Start()
    {
        base.commonStart();
    }

    // Update is called once per frame
    void Update()
    {
        base.commonUpdate();
    }
}
